# Instrucciones para agentes

Leer y seguir `CLAUDE.md` en la raíz: es la guía canónica de Ruts68. Consultar además `front/CLAUDE.md` o `back/CLAUDE.md` según el área. No duplicar reglas de negocio en este archivo.

El demo `index.html` se conserva como referencia. `front/` y `back/` contienen la aplicación real. No trabajar ni desplegar en el repositorio Kuvvi: solo es referencia de arquitectura.
