{
  "name": "ruts68",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "front",
    "back"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "db:local": "node scripts/database.mjs",
    "db:migrate": "npm run db:migrate -w back",
    "typecheck": "npm run typecheck --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm test -w back",
    "test:integration": "node scripts/test-integration.mjs",
    "routes:generate": "npm run routes:generate -w back",
    "routes:check": "npm run routes:check -w back",
    "preflight:production": "node scripts/preflight-production.mjs",
    "mail:local": "node scripts/mail-local.mjs",
    "demo:seed": "node scripts/seed-demo.mjs"
  },
  "devDependencies": {
    "embedded-postgres": "18.4.0-beta.17",
    "prettier": "^3.6.2"
  }
}
